/* =====================================================================
   FOM STUDIOS — Firebase configuration
   ---------------------------------------------------------------------
   Paste the config from YOUR Firebase project below (you only do this once).

   Where to find it:
     Firebase console  →  Project settings (⚙ gear, top-left)
                       →  scroll to "Your apps"  →  the Web app  →  "SDK setup and configuration"
                       →  choose "Config"  →  copy the values into the fields below.

   Until these are filled in, the Dashboard & Database run in LOCAL-ONLY mode
   (data stays on this device, no login). Once filled in, they switch to
   cloud sync + login automatically.
   ===================================================================== */
var firebaseConfig = {
  apiKey:            "PASTE_API_KEY_HERE",
  authDomain:        "PASTE_PROJECT.firebaseapp.com",
  projectId:         "PASTE_PROJECT_ID",
  storageBucket:     "PASTE_PROJECT.appspot.com",
  messagingSenderId: "PASTE_SENDER_ID",
  appId:             "PASTE_APP_ID"
};
